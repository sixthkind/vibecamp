import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sixthkind.axiom',
  appName: 'Axiom',
  webDir: 'dist',
  ios: {
    scheme: 'Axiom'
  }
};

export default config;

